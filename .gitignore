# macOS / FUSE
.DS_Store
.fuse_hidden*

# Obsidian (config do vault — não pertence ao repo)
.obsidian/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
pytest-cache-files-*
.venv/
venv/
dist/
build/

# Runtime do motor (logs vivos, banco de checkpoint, workspaces de run)
log.jsonl
log 2.jsonl
motor.db
motor.db-*
runs/
.log.jsonl.lock
motor/logs/
.langgraph_api/
motor/motor_painel/_testdel

# Generated documentation corpus
motor/exemplos/rag-docs-metafabrica.jsonl

# Node (futuros consumidores/superfícies)
node_modules/

# Segredos — NUNCA commitar chaves de API (LiteLLM, provedores, etc.)
.env
*.env
!*.env.example

# graphify (saída local do /graphify, não pertence ao repo)
graphify-out/

# Estado local de agentes/navegador
.agents/
.claude/
.browser-profile/
.maestri/

# Café do_KEEP_AWAKE do LiteLLM (estado de runtime, não pertence ao repo)
caffeinate.log
caffeinate.pid
infra/litellm/caffeinate.log
infra/litellm/caffeinate.pid
