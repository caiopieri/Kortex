{
  "_comentario": "SHAKEDOWN free DIRETO (sem opencode). UM modelo só (NVIDIA llama-3.3-70b via HTTP openai-compat) pra isolar a variável depois que o opencode travou (database is locked + timeout em fan-out paralelo). Transporte openai-compat = request HTTP, sem cold-start nem banco local, concorrência não trava. Exige NVIDIA_API_KEY no ambiente + certificados do Python do Mac (Install Certificates.command). 'padrao':'nenhum' = sem claude/codex (inerte até o HANDOFF-CODEX-SEM-CAROS entrar; até lá, em FALHA ainda cai no claude — rode olhando o log por modelo.fallback). Quando este completar, eu monto a escada direta completa (llama + qwen-next juiz + openrouter).",

  "padrao": "nenhum",

  "provedores": {
    "nvidia": {
      "base_url": "https://integrate.api.nvidia.com/v1",
      "api_key_env": "NVIDIA_API_KEY",
      "custo_ordem": 1
    }
  },

  "tiers": {
    "simples":  "nvidia/meta/llama-3.3-70b-instruct",
    "media":    "nvidia/meta/llama-3.3-70b-instruct",
    "complexa": "nvidia/meta/llama-3.3-70b-instruct"
  },

  "pins": {
    "planner":     "nvidia/meta/llama-3.3-70b-instruct",
    "verifier":    "nvidia/meta/llama-3.3-70b-instruct",
    "evaluator":   "nvidia/meta/llama-3.3-70b-instruct",
    "synthesizer": "nvidia/meta/llama-3.3-70b-instruct"
  }
}
